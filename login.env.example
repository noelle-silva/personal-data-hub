# 登录认证配置文件
# 复制此文件为 login.env 并根据需要修改配置

# 登录用户名
LOGIN_USERNAME=admin

# 登录密码（bcrypt散列值）
# 生成方法：
# 1. 在 backend 目录运行：node -e "const bcrypt = require('bcrypt'); bcrypt.hash('你的密码', 10).then(console.log)"
# 2. 或者使用在线 bcrypt 生成器：https://bcrypt-generator.com/
# 示例（密码为 "admin123"）：
LOGIN_PASSWORD_HASH=$2b$10$N9qo8uLOickgx2ZMRZoMye5YrjH1KkYh5L4G5Q6Q7Q8Q9Q0Q1Q2Q

# JWT 配置
# JWT 签名密钥（请更改为强随机字符串）
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# JWT 过期时间（默认24小时）
JWT_EXPIRES_IN=24h

# 管理员用户ID（必须是有效的24位十六进制ObjectId字符串）
# 如果未配置，将使用默认值 000000000000000000000001
ADMIN_USER_ID=000000000000000000000001

# =========================
# Cookie 登录态（生产环境推荐）
# =========================
# 说明：后端会把 JWT 放进 HttpOnly Cookie（前端 JS 读不到），更适合公网部署。

# 登录态 Cookie 名称
AUTH_COOKIE_NAME=pdh_auth

# CSRF Cookie 名称（Double Submit Cookie，用于 POST/PUT/PATCH/DELETE）
CSRF_COOKIE_NAME=pdh_csrf

# Cookie 安全选项
# - 生产环境 HTTPS 下建议 true
# - 本地开发可保持 false
COOKIE_SECURE=false

# SameSite 选项：lax / strict / none（none 必须配合 COOKIE_SECURE=true）
COOKIE_SAMESITE=lax

# 可选：设置 Cookie 域（比如 example.com），不需要就留空
# COOKIE_DOMAIN=example.com

# 可选：如果你还有旧客户端需要读取 token，可以开启（不推荐公网）
AUTH_RETURN_TOKEN=false

# =========================
# CORS（前后端不同源时必配）
# =========================
# 逗号分隔，例如：
# CORS_ORIGINS=https://app.example.com,https://admin.example.com
# CORS_ORIGINS=
