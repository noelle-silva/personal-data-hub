# 复制本文件配置mongodb链接数据
# MongoDB数据库连接字符串
# 使用本地Docker容器中的MongoDB，包含认证信息
MONGODB_URI=mongodb://username:password@localhost:port/数据库名称?authSource=admin

# MongoDB集合名称配置
# 指定文档数据存储的集合名称，默认为documents
DOCUMENT_COLLECTION=documents

# 指定引用体数据存储的集合名称，默认为quotes
QUOTE_COLLECTION=quotes

# 指定附件元数据存储的集合名称，默认为attachments
ATTACHMENT_COLLECTION=attachments

# 指定自定义页面数据存储的集合名称，默认为custom-pages
CUSTOM_PAGE_COLLECTION=custom-pages

# 指定AI系统提示词数据存储的集合名称，默认为AI-prompts
# 如果未配置，系统将使用默认值 "AI-prompts"
AI_PROMPTS_COLLECTION=AI-prompts