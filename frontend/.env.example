# Personal Data Hub (Frontend) - .env Example
#
# 前端基于 Create React App（react-scripts），只会读取 `frontend/.env`（以及 .env.production 等）。
# 注意：只有以 `REACT_APP_` 开头的变量会被注入到前端构建产物里。

# 关闭自动打开浏览器（Tauri 桌面端开发更顺手）
BROWSER=none

# 可选：开启性能日志（见 frontend/src/utils/markdownRenderer.js）
REACT_APP_ENABLE_PERFORMANCE_LOG=false
